[project]
name = "workflows"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "langgraph",
    "shared",
    "agents",
]

[tool.uv.sources]
shared = { workspace = true }
agents = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"