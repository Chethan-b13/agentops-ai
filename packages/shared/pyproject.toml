[project]
name = "shared"
version = "0.1.0"

dependencies = [
    "pydantic-settings",
    "sqlalchemy",
    "psycopg[binary]",
    "pygithub>=2.9.1",
    "langfuse==2.60.0",
    "langchain-ollama",
    "langchain-google-genai",
    "agents",
]

[tool.uv.sources]
agents = { workspace = true }